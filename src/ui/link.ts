import { ClassName } from "../types/ClassName";
import type { LinkUI } from "../components/Link/src/types/LinkUI";

const linkUI = {
    nodes: {
        root: [ClassName['d-inline-flex']],
    },
} satisfies LinkUI;

export default linkUI;
