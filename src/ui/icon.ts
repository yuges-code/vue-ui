import { ClassName } from "../types/ClassName";
import type { IconUI } from "../components/Icon/src/types/IconUI";

const iconUI = {
    nodes: {
        root: [ClassName['d-flex']],
    },
} satisfies IconUI;

export default iconUI;
