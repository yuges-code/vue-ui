import type { AppUI } from "../../../components/App/src/types/AppUI";

export const nodes = {
    root: [
        'd-flex',
        'flex-column',
        'position-relative',
        'bg-default',
        'text-default'
    ],
} satisfies AppUI['nodes'];
